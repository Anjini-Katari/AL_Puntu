# AL_Puntu (Anjini Katari and Jacc Chen)

# ALTester
 * sortTester iterates through each value in the array
   and checks whether it is less than or equal to the 
   value of the next index. If it is, it will return 
   true, if not, it will return false. 

# OrderedArrayList
 * toString() 
    * returns a string representation of array data

 * remove(int i)
    * removes an integer from the array 

 * size()
    * returns the size of the array 

 * get(int i)
    * returns the value in the provded index of an array

 * addLinear(Integer newVal)
    * adds a value into the array, while ensuring the 
      array continues to be in an order that acsends in 
      value

